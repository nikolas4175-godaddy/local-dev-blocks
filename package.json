{
  "name": "local-dev-blocks",
  "version": "0.1.0",
  "description": "A simple plugin for building multiple WP blocks in a dev environment.",
  "author": "Nik McLaughlin",
  "license": "GPL-2.0-or-later",
  "main": "build/index.js",
  "scripts": {
    "build": "wp-scripts build --experimental-modules --source-path=blocks",
    "format": "wp-scripts format",
    "lint:css": "wp-scripts lint-style",
    "lint:js": "wp-scripts lint-js",
    "packages-update": "wp-scripts packages-update",
    "plugin-zip": "wp-scripts plugin-zip",
    "start": "wp-scripts start --experimental-modules --source-path=blocks"
  },
  "devDependencies": {
    "@wordpress/scripts": "^31.0.0"
  },
  "dependencies": {
    "@wordpress/interactivity": "^6.35.0"
  }
}
